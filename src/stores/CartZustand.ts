// import useCart from "@/hooks/CRUD/useCart";
// import { ICart } from "@/types/api";
// import { create } from "zustand";

// type CartStore = {
//   carts: Partial<ICart>[];
//   getCarts: () => void;
//   // updateCart: () => void,
//   // removeAll: () => void
// };

// const { carts, getCarts, updateCart } = useCart();

// export const useCartStore = create<CartStore>((set) => ({
//   carts: carts,
//   getCarts,
//   // updateCart:updateCart()
//   // add: () => set((state) => ({ cart: state.cart + 1 })),
//   // remove: () => set((state) => ({ cart: state.cart - 1 })),
//   // removeAll: () => set({ cart: 0 }),
// }));
